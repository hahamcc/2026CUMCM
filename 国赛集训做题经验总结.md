# 国赛集训做题经验总结

> 适用对象：两名建模手（一人主方案、一人主代码）与一名论文手组成的三人队伍。  
> 使用范围：从正式比赛选定题目后开始，覆盖读题、定模、数据处理、编程、验证、协作、写作与交付。  
> 核心目标：防止题意口径和模型方向走偏，同时避免在求解、验证和深化上投入过度。

---

## 一、复盘结论

经过本轮集训，团队已经积累了评价、统计分析、预测、机理建模、优化运筹、随机情景、风险分析和几何规划等多类题目的经验。当前最需要提升的已经不是“知道多少模型”，而是把一道复杂题稳定收敛为一条正确、完整、能按时提交的主线。

从 2010D、2012A、2016C、2019A、2020B、2023B 和 2024C 的训练记录看，团队最值得警惕的风险模式是：

> 前期关键口径没有完全锁定，后期试图用更复杂的求解、更大量的验证和更多图表进行补偿。

正式比赛应采用相反的顺序：

> 先锁定题意口径，再跑通基础闭环；验证只服务决策，深化必须限量；最后以唯一可信的结果源完成论文交付。

对于主方案建模手而言，核心职责不是亲自完成最多计算，而是始终守住五件事：

1. 团队解决的是不是题目真正要求的问题；
2. 关键假设有没有超出题面和数据能够支持的范围；
3. 模型能不能被代码手准确实现；
4. 结果和结论是否经过有效验证；
5. 什么时候必须停止继续研究，转入写作与交付。

---

## 二、读题与口径：先保证解决的是正确问题

### 2.1 每一问先写清“最终到底交什么”

2016C 的训练中，问题三一度把“补全曲线采样点”误当成最终目标，后来才明确真正要求的是预测状态 3 从最后观测点到 9V 的剩余时间。这类偏差非常危险：模型可能做得很好，但解决的是一个中间任务，而不是题目要求的最终任务。

正式比赛中，每一问应先写清以下六项：

1. **研究对象**：预测、评价或优化的对象是什么；
2. **最终输出**：需要给出数值、方案、曲线、排序、解释还是评价；
3. **可用信息**：真正求解时能够使用哪些数据，哪些属于未来或未知信息；
4. **变量划分**：决策变量、状态变量、输入参数分别是什么；
5. **硬约束**：哪些条件必须满足，哪些只是背景或评价倾向；
6. **验证方式**：怎样判断结果可信、模型没有解决错问题。

#### 赛中动作

- 三名队员分别用一句话复述每一问的任务；
- 主方案建模手汇总为团队统一版本；
- 代码手必须能够说清输入、输出和约束；
- 论文手必须能够说清本问为什么需要建模、最后回答什么。

三个人的复述不一致时，不进入主体实现。

### 2.2 区分题面事实、合理解释和团队设定

2024C 中出现了多项必须由团队选择的建模口径，例如：

- 某季没有历史种植，不等于该季市场需求为零；
- 销售容量采用年度共享还是按季次分配；
- 是否要求每个可种植季次必须种满；
- 豆类轮作按“出现过豆类”还是按累计豆类面积覆盖；
- 混种地块的末季状态不能只记录一种作物；
- 替代、互补和相关强度不是附件估计值，而是情景设定。

这些内容如果不加区分地写成“题目规定”，后续模型即使形式严密，基础也可能错误。

建议建立统一的口径表：

| 类型 | 含义 | 处理方式 |
|---|---|---|
| 题面事实 | 题目明确给出的条件 | 必须严格实现 |
| 合理解释 | 题面有歧义，需要团队选定口径 | 写明理由，必要时设置对照情景 |
| 团队设定 | 题面没有给出，由团队量化 | 必须标注，并进行敏感性或边界说明 |
| 待确认项 | 尚不能拍板的问题 | 未解决前不得写入代码默认值 |

### 2.3 口径修改必须进行全链路影响检查

2024C 的销售容量口径曾从“年度共享”调整为“季次容量”。这种修改不只是改一句论文文字，它会同时影响：

- 预处理工作簿；
- 优化模型中的销量约束；
- 求解结果；
- 图表和统计指标；
- 问题二、问题三的继承关系；
- 摘要、正文和结论。

#### 赛中动作

每次修改关键口径，都记录：

```text
修改内容：
修改原因：
受影响的模型与文件：
旧结果是否作废：
需要重新生成的表格与图形：
论文中需要同步的位置：
```

#### 止损规则

基础口径冻结后，只有发现以下问题才允许推翻：

- 题意误读；
- 硬约束遗漏；
- 代码与口径矛盾；
- 结果明显违背题面或常识；
- 当前口径使某一问无法被真正回答。

不得因为“另一种解释看起来更高级”或“结果可能更漂亮”而反复重建模型。

---

## 三、定模：基础闭环比高级模型更重要

### 3.1 先建立最简单但完整的主体模型

老师反馈中反复强调的是“最基础、最简单、最易懂、最准确”，而不是模型名称高级。

一个合格的基础模型必须完整包含：

- 集合、参数与变量；
- 目标函数；
- 全部硬约束；
- 初始条件和边界条件；
- 求解方法；
- 最终输出；
- 最低验证方式。

2024C 的成功之处不是一开始就引入相关随机情景和风险指标，而是先把土地适宜性、轮作、豆类覆盖、水浇地模式、面积约束和销售闭合建成可运行的混合整数线性规划。后面的风险模型因此有了可靠底座。

2010D 和 2019A 则提供了反面经验：前期提取了大量题面、附件和资料，也进行了较细的机理讨论，但完整闭环形成较慢。正式比赛中，资料读取、文献检索和概念讨论不能无限替代第一个可运行模型。

### 3.2 候选模型要比较，但不能无限扩张

候选方案至少比较以下方面：

| 比较维度 | 需要回答的问题         |
| ---- | --------------- |
| 题意匹配 | 是否直接回答题目要求      |
| 假设负担 | 是否需要题面没有提供的额外假设 |
| 数据支持 | 所需数据和参数是否具备     |
| 实现成本 | 比赛时间内能否可靠实现     |
| 验证方式 | 如何判断不是“看起来合理”   |
| 可解释性 | 论文中能否讲清楚        |
| 回退能力 | 复杂方案失败后能否回到简单模型 |

不要为了凑出多个方案而硬列多个复杂模型。有效的候选梯度是：

> 简单基线 → 可解释主模型 → 针对具体缺陷的增强模型。

### 3.3 深化必须有明确准入条件

深化模型只有同时满足以下四项，才进入主线：

1. 基础模型确实暴露了具体缺陷；
2. 题面、数据或可靠资料支持改进方向；
3. 改进有可以计算的验证方法；
4. 剩余时间足够完成代码、结果和论文闭环。

缺少任何一项，就停止深化。

### 3.4 不要把算法当成模型

“使用遗传算法、粒子群、Gurobi、蒙特卡洛或神经网络”只说明如何求解，没有说明究竟求什么。

论文和交接材料必须先给出模型总结：

> 决策变量是什么，优化目标是什么，约束是什么，算法如何在可行域内寻找解。

主要算法需要分步骤说明，次要的插值、求根、绘图和辅助统计方法可以简写。不能用复杂算法流程图掩盖模型本身没有定义完整的问题。

---

## 四、数据与预处理：先理解数据代表什么

### 4.1 零值、缺失和未观测必须区分

训练中反复出现了同类问题：

- 2016C 中，曲线结束后的尾部空白不是普通缺失值；
- 2024C 中，某季没有历史种植不代表该季没有市场需求；
- 古代玻璃题中，未检测到不一定等于真实含量为零；
- 混种地块只保留“最后一种作物”，会丢失初始重茬状态。

正式比赛中应把空白区分为：

- 真缺失；
- 结构性空白；
- 未发生；
- 未检测到；
- 不适用；
- 历史未观察到。

未经语义判断，不得直接补零、插值或删除。

### 4.2 数据处理不能照搬固定流程

标准化、异常值删除、平滑和降维都不是必做步骤。培训笔记中“聚类和分类都不标准化”“尽量不用 Min-Max”等说法过于绝对。

正确原则是：

> 是否处理，取决于算法是否受尺度影响、数据是否具有特殊结构、异常值是否正是题目要求研究的对象。

每项预处理都应回答：

1. 它解决什么具体问题；
2. 是否破坏原始含义；
3. 处理参数是否只由训练数据得到；
4. 改变处理规则后结论是否变化。

### 4.3 保留原始数据与统一预处理源

正式比赛中应做到：

- 原始附件只读保存，不直接覆盖；
- 清洗规则集中记录；
- 单位、编码、地块类型和时间层级统一；
- 模型只读取统一的预处理结果；
- 关键派生字段能追溯到原始数据；
- 论文中的统计量与代码读取的数据源一致。

---

## 五、从模型到代码：必须先形成可执行规格

### 5.1 代码手不能依靠聊天记录猜模型

主方案建模手交付给代码手的内容至少包括：

- 输入文件、字段和单位；
- 索引集合；
- 参数及其来源；
- 决策变量和取值范围；
- 目标函数；
- 每条约束；
- 时间、空间或事件推进顺序；
- 初始条件和边界情况；
- 输出表结构；
- 一个可手算的小例子；
- 已知风险和待确认点。

交付后让代码手反向复述。复述不出来，说明规格仍不完整。

### 5.2 程序有输出不等于模型正确

2020B 的训练很好地区分了三个概念：

- **正确性**：代码是否实现了预定模型；
- **最优性**：是否证明或接近最优；
- **效率**：计算是否更快、更省资源。

当时剪枝后变量和约束显著减少，但求解时间没有加快，因此只能说模型规模缩小，不能宣称算法效率提高。

每个最终模型至少完成：

- 小规模可手算或可枚举对照；
- 资源、质量、销量、概率或预算闭合检查；
- 全部硬约束逐项审计；
- 极端输入和边界测试；
- 输出表与内部变量复算；
- 与简单基线或原方案比较。

### 5.3 求解器结论必须符合证据等级

2024C 第一问某方案的第二阶段 MIP gap 仍在 5% 左右，所以只能称为“当前最优可行方案”，不能称为严格最小管理单元数。

正式比赛采用以下表述：

| 证据 | 合理表述 |
|---|---|
| 有严格证明 | 最优解 |
| gap 足够小 | 近最优解 |
| 只找到满足约束的较好解 | 当前最优可行方案 |
| 启发式或局部搜索 | 当前搜索范围和判据下的推荐方案 |

不得因为结果数值精确到很多小数，就夸大其最优性或现实准确性。

---

## 六、验证：验证要服务决策，不追求数量

### 6.1 先问“这次验证防什么错”

2016C 最有价值的收获不是某个拟合函数，而是验证结构必须模拟真实未知：

- 随机拆分时间点可能泄漏未来信息；
- 未知电流应留出整条电流曲线；
- 尾部预测应在人为截断的完整历史曲线上回测；
- 内插验证不能证明外推能力。

验证设计应与题目最终使用场景一致，不能只选择方便计算或让指标更好看的划分方式。

### 6.2 独立评价比训练指标更重要

2024C 第三问中，第二阶段 CVaR 方案在训练情景上有所改善，但在两批独立评价中反而不如第一阶段方案，最终正确地退回第一阶段方案。

这说明：

> 深化模型是否保留，要看独立评价能否重复其优势，而不是只看训练目标有没有提高。

同时，问题三方案提高了最差利润、CVaR 和低分位利润，但标准差略有上升。因此只能说“下行收益保障增强”，不能写成“全面降低利润波动”。

### 6.3 最低验证包

每一问至少完成：

1. 硬约束与边界检查；
2. 一个简单基线或原方案对照；
3. 一个针对核心假设的敏感性或独立验证；
4. 关键结果复算；
5. 一个最难解释结果的原因分析。

完成这五项后，再判断是否确有必要继续扩展。

### 6.4 明确求解和验证的止损规则

你们在集训中最容易浪费时间的环节，是求解与验证过深。正式比赛采用以下停止条件：

- 新实验必须对应一个可能改变模型选择或主要结论的问题，否则不做；
- 两次独立评价得到相同方向，且没有发现新约束错误，原则上不再增加随机种子和情景批次；
- 灵敏性分析不改变主要排序、结构和结论时，只保留代表性结果；
- 深化模型只改善训练指标、独立评价无法重复时，立即回退；
- 求解器继续运行预计不会改变推荐方案时，冻结当前可行解，并降低最优性表述；
- 一张图不能支撑一句新的结论，就不画；
- 验证已能说明“约束正确、结果可复算、结论能抵抗一个关键扰动”后，优先转向论文闭环。

2024C 的独立情景评价帮助否决了 CVaR 候选方案，这是有效验证；当模型排序和结论已经稳定后，继续增加情景数量的边际价值就很低。

---

## 七、三人协作：建立唯一可信的结果源

### 7.1 三名队员的固定责任

#### 主方案建模手

- 统一题意和口径；
- 组织候选方案比较并拍板；
- 输出建模规格；
- 设计验证问题和停止条件；
- 审核模型、代码和论文是否一致。

#### 代码建模手

- 编写可复现程序；
- 完成微型测试和约束审计；
- 生成结构化结果；
- 保存求解日志和结果清单；
- 维护最新代码版本。

#### 论文手

- 把模型组织成评委能够理解的逻辑；
- 追问数字、公式和图表来源；
- 统一符号、单位和精度；
- 保证摘要、正文和结论一致；
- 整理支撑材料与代码附录。

不能形成“建模手想模型、代码手黑盒跑数、论文手最后猜着写”的流水线。

### 7.2 固定同步节点

建议至少设置五次短同步：

1. 题意与口径统一后；
2. 模型规格交给代码手前；
3. 第一版结果产生后；
4. 决定是否深化前；
5. 最终结果与论文冻结前。

每次同步只回答：

```text
已经确认什么？
尚未确认什么？
当前最大风险是什么？
下一步最小动作是什么？
哪些旧结果已经作废？
```

### 7.3 任何关键修改必须主动广播

2024C 中出现过多人版本并行、本地备份后拉取、结果文件未统一跟踪等情况。老师也特别强调，修改确认后应及时合并，并明确通知队友。

正式比赛应固定：

- 一份题意口径文件；
- 一份当前建模规格；
- 一份最终结果清单；
- 一名合并负责人；
- 同一核心文件同一时段只由一人修改；
- 修改后说明“改了什么、哪些数字作废、论文哪里要同步”。

论文手只能从最终结果清单取数，不能从聊天截图、旧 Excel 或个人目录中自行选择数字。

### 7.4 代码与文件管理

- 不在代码中写个人电脑绝对路径；
- 文件夹和输出不得包含个人或学校信息；
- 代码文件数量尽量少，入口清楚；
- 修改确认后及时提交并告知队友；
- 代码附录必须来自最终冻结版本；
- 提交前对论文、支撑材料、代码和结果进行版本对应检查。

---

## 八、论文表达：让评委不用替团队补逻辑

### 8.1 每一问必须形成完整叙事闭环

固定采用以下顺序：

> 为什么这样处理 → 建立什么模型 → 如何求解 → 得到什么结果 → 怎样验证 → 结果说明什么。

模型总结不能只有公式，还应解释变量含义、取值范围和逻辑。说明性文字放在公式前，告诉评委为什么需要该公式。

结果部分不能只报数字，还要回答：

- 数字是否直接回答题目；
- 为什么出现这种结构；
- 与基线相比有什么变化；
- 结论依赖哪些假设；
- 哪些内容不能向现实无限外推。

### 8.2 主要算法写清步骤，次要算法简写

主要算法应说明：

1. 输入和初始化；
2. 解的表示方式；
3. 迭代、搜索或更新规则；
4. 可行性处理；
5. 终止条件；
6. 最终结果如何得到。

辅助性的插值、求根、绘图和数据读取方法，只需说明名称、使用理由和必要参数，不应挤占主体模型篇幅。

### 8.3 图表必须承担论证任务

图表规则可以压缩为：

- 一图一结论；
- 字号足够大；
- 不只依靠颜色区分，线型和标记也应不同；
- 图后必须有文字解释；
- 表格突出关键数字时，优先使用加粗或字体变化，不依赖大面积填色；
- 不在正文中使用“红线”“黄色区域”等依赖颜色的指代；
- 重要图应提前检查打印效果。

流程图和机理示意图可以让 AI 提供草案，但最终应检查字体、清晰度和打印效果。

### 8.4 结论必须符合证据边界

推荐使用：

- “由 A 降至 B，变化 C%”；
- “在当前范围和评价判据下的推荐方案”；
- “在独立模拟情景中保持相同方向”；
- “下行收益指标提高，但标准差略有上升”。

避免使用：

- “显著改善”但没有指标；
- “全局最优”但没有证明；
- “现实验证通过”但只做了模拟；
- “模型非常稳定”但只换了一个参数；
- “相关系数由数据得到”但实际上是团队设定。

### 8.5 摘要最后写，但关键素材提前维护

摘要必须包含：

- 每问采用的模型或方法；
- 关键结果数值；
- 主要验证；
- 最终结论及适用边界。

建模过程中持续维护“本问一句话结论”和“关键数字清单”，最后再组装摘要。否则容易出现摘要与正文数字不一致。

---

## 九、AI 协作：把 AI 当审查副驾驶

### 9.1 AI 适合承担的工作

- 检查题意歧义和约束遗漏；
- 比较候选模型的隐含假设；
- 生成极端情形、反例和测试；
- 检查模型、代码与论文是否一致；
- 帮助把复杂叙述改成评委易读的表达；
- 根据团队给出的模型规格补充实现框架；
- 以陌生评委视角进行审稿。

### 9.2 AI 不应替代的工作

- 三个人的首次独立读题；
- 第一版候选方案；
- 关键口径拍板；
- 模型深化取舍；
- 最终结论责任。

### 9.3 AI 输出必须分类核验

AI 给出的内容应被分类为：

- 题面事实；
- 外部事实；
- 数学推导；
- 建模假设；
- 待验证建议。

尤其是相关系数、概率分布、替代关系、参数范围和所谓“最合适模型”，不能因为 AI 表述完整就直接采用。

2024C 中将“关系方向”和“参数强度”分开，并明确联动权重是团队设定，这种证据边界应继续保留。

---

## 十、从各训练题中应带走的核心经验

| 训练题 | 最值得保留的经验 | 需要避免的问题 |
|---|---|---|
| 2010D 评价 | 指标必须能对应经济性、舒适性和安全性，并能从图纸或数据计算 | 不要长时间停留在资料提取而没有形成最小评价模型 |
| 2012A 葡萄酒 | 区分样本层级、评委重复测量、标签不确定性；重视对照和稳定性 | 正式比赛不要把所有可做检验都做一遍，应保留与主结论相关的证据 |
| 2016C 预测 | 先设计与真实未知一致的验证；区分内插、未知组和尾部外推 | 不能随机拆分时序点，也不能把中间输出误当最终任务 |
| 2019A 高压油管 | 机理模型必须写出质量守恒、微分方程、初值和控制量 | 文献、图形和物理讨论不能无限拖延第一问闭环 |
| 2020B 穿越沙漠 | 用小实例验证模型；区分正确性、最优性和效率 | 模型规模减小不代表求解一定更快 |
| 2023B 测线规划 | 几何关系、覆盖约束和搜索算法必须统一；关键图要解释空间含义 | 不能让复杂搜索代替几何约束的清楚表达 |
| 2024C 种植优化 | 口径分层、统一预处理、两阶段模型、独立情景评价和诚实的 gap 表述 | 防止口径反复、版本分叉，以及情景、种子、图表和验证无限扩张 |

---

## 十一、主方案建模手的赛中检查句

每完成一个阶段，主方案建模手追问：

1. 我们回答的是题目真正要求的问题吗？
2. 当前结论中，哪些是题面事实，哪些是团队设定？
3. 代码手能否不看聊天记录就实现模型？
4. 新实验是否可能改变模型或结论？
5. 现在继续优化的收益，是否大于完成论文闭环的收益？

如果第五问的答案是否定的，就应停止继续求解。

主方案建模手还应能够在三分钟内回答：

- 为什么采用当前模型；
- 为什么没有采用主要替代方案；
- 模型最关键的假设是什么；
- 结果怎样通过验证；
- 当前结论最大的适用边界是什么。

---

## 十二、正式比赛阶段检查表

### 12.1 题意与口径

- [ ] 每一问的对象、输入、输出和交付已经明确；
- [ ] 三名队员可以一致复述每一问；
- [ ] 题面事实、合理解释和团队设定已经分开；
- [ ] 问间关系和前问输出已经明确；
- [ ] 争议口径已经记录，未偷偷写入代码默认值。

### 12.2 定模

- [ ] 已经有一个最小可运行的基础模型；
- [ ] 变量、目标和约束能够完整总结；
- [ ] 已比较主要候选方案并记录淘汰理由；
- [ ] 深化针对的是基础模型的具体缺陷；
- [ ] 当前模型在剩余时间内能够完成代码和论文闭环。

### 12.3 数据与实现

- [ ] 原始数据未被覆盖；
- [ ] 单位、缺失、结构性空白和异常已经检查；
- [ ] 预处理规则有统一来源；
- [ ] 建模规格已经交付给代码手并完成复述；
- [ ] 已通过小实例、边界和硬约束检查；
- [ ] 输出表能够从模型变量复算。

### 12.4 验证与停止

- [ ] 验证结构与真实未知一致；
- [ ] 已有基线或原方案对照；
- [ ] 已完成一个关键敏感性或独立评价；
- [ ] 求解器 gap 和结论表述一致；
- [ ] 新实验确实可能改变决策；
- [ ] 达到停止条件后已经冻结模型和结果。

### 12.5 协作与写作

- [ ] 团队只使用一份题意口径和最终结果清单；
- [ ] 所有关键修改已经通知队友；
- [ ] 论文手使用的是最新冻结结果；
- [ ] 每一问都有“模型—求解—结果—分析—验证”闭环；
- [ ] 一图一结论，图表与文字相互对应；
- [ ] 摘要、正文和结论中的数字一致；
- [ ] 代码附录与最终代码版本一致；
- [ ] 文件中不存在个人、学校或本地绝对路径信息。

---

## 十三、最终应记住的十条规则

1. **先定义交付，再讨论模型。**
2. **题面事实、合理解释和团队假设必须分开。**
3. **口径修改必须同步数据、模型、代码、图表和论文。**
4. **先让基础模型形成闭环，再考虑深化。**
5. **算法不能代替变量、目标函数和约束的模型定义。**
6. **代码完成后先审约束和边界，不先看漂亮结果。**
7. **验证必须模拟模型实际面对的未知信息。**
8. **深化只保留能够在独立评价中重复的改进。**
9. **全队只使用一个题意源、一个模型源和一个结果源。**
10. **当继续研究不能改变决策时，立即停止，转向写作和交付。**

---

## 结语

这轮集训真正形成的能力，不是记住了多少模型名称，而是开始学会：

- 把自然语言题目压缩为可计算任务；
- 区分事实、解释和假设；
- 从简单基线逐步建立完整模型；
- 用与真实未知一致的方式验证；
- 诚实说明最优性、稳健性和现实边界；
- 在有限时间内判断什么时候已经足够可靠。

正式比赛中，团队不必追求把每个方向都研究到极致。更重要的是做出一套有依据的选择，使题意、模型、代码、结果、验证和论文表达形成同一条逻辑链，并在截止时间前稳定地交付出来。

---

## 参考材料

- 当前工作区中的模型知识笔记、优秀论文复盘和各题训练记录；
- `10_Projects/CUMCM2016C预测训练/09_预测类数学建模通用作战手册.md`；
- `10_Projects/国赛第二阶段方案负责人训练/训练总览.md`；
- `10_Projects/国赛第二阶段方案负责人训练/单题四天训练流程.md`；
- `D:/虚拟C盘/2024C/C题_题意与约束口径_讨论稿.md`；
- `D:/虚拟C盘/2024C/Q1/第一问_论文叙事交付稿.md`；
- `D:/虚拟C盘/2024C/Q2/第二问建模思路.md`；
- `D:/虚拟C盘/2024C/Q3/第三问论文叙事与结果交接稿.md`；
- 培训材料《培训知识点笔记》《全流程总结》《竞赛须知和时间安排》。
