# 数学建模竞赛：独特性与核心图表准入规程

> 本文件是根目录 `AGENTS.md` 的专项补充。凡涉及核心模型或求解设计、所谓创新与深化、正文核心图设计、结果可视化或图表审稿，AI 必须先读取本文件。若与 `AGENTS.md` 冲突，以 `AGENTS.md` 为准。

## 1. 目标与边界

本规程解决两类问题：

1. **方案同质化**：AI 容易给不同队伍相似的模型清单、算法组合、验证套路和论文叙事；
2. **图表模板化**：图可能外观完整，却没有增加答案、解释或验证证据，并出现常见的双轴、大片阴影、箭头框和过量标注。

独特性不等于冷门、复杂或从未出现。比赛中的有效独特性是：

> 从本题独有的数据、约束、矛盾、边界、反常现象或验证风险出发，对可靠基础模型作有证据、可验证、会改变答案或解释的改造。

不得搜索、获取、比较或模仿当届赛题的他人方案。AI 只能判断“通用模板风险”，不能声称知道真实重复率。

## 2. 谁先提出独特性

- 团队先完成独立读题，并至少指出一个本题特征、疑点或反常现象；AI 不替代首次独立思考。
- 团队暂时没有切口时，AI 先做题目指纹诊断，只挖掘异常数据、特殊约束、冲突目标、边界情形、决定性中间结论和验证缺口，不直接抛出所谓创新模型。
- 团队选定切口后，AI 才能扩展候选、反驳、推导、实现和验证。
- 可靠基线始终保留；题目特异改造失败时立即回退，不为维持独特性修改指标、删数据或掩盖反例。

## 3. 题目指纹卡

核心建模前填写：

```text
本题独有的数据结构：
本题独有的约束、边界或事件顺序：
真正冲突的目标或评价口径：
最反常、贴边或难解释的现象：
最可能导致结论失效的风险：
常规模型不能自然处理的部分：
可能决定模型推进的非显然结论：
团队愿意深入的切口：
```

若只能写出“数据较复杂、因素较多、需要优化、存在不确定性”等多数赛题都适用的话，题目指纹尚未完成。

## 4. 核心方案独特性准入

### 4.1 独特性可以出现在哪些层面

- **口径**：对歧义定义、评价标准、时间或空间尺度作有依据的选择；
- **变量与约束**：加入本题特有状态、事件、资源关系、逻辑或边界；
- **模型结构**：利用守恒、几何、网络、时序、分层或相关机制改造基础模型；
- **求解**：利用问题结构缩小可行域、分解问题、构造初解或安排搜索顺序；
- **验证**：针对本题最危险的假设设计反例、退化情形、独立情景或解析检查；
- **表达**：用题型专属的空间、时序、机制或决策结构呈现证据。

一篇论文至少在两个相互支撑的关键层面体现题目特异性。算法名称、参数数量和图表配色不单独构成独特性。

### 4.2 独特性卡

```text
可靠基础模型：
触发改造的题目指纹：
具体增加、删除或改变了什么：
改动层面：口径 / 变量约束 / 模型结构 / 求解 / 验证 / 表达
题面、数据、首版结果或证明依据：
该改动会改变哪个答案、约束、排序、风险或解释：
验证方法与通过判据：
失败时回退方案：
通用模板风险：高 / 中 / 低
```

### 4.3 五项审查

1. **名词替换检查**：把研究对象名称换成其他赛题后，方案是否仍可原样使用？若是，模板风险高。
2. **证据追溯检查**：每项改动能否追溯到题面、数据、决定性证明、首版缺陷或验证风险？
3. **必要性检查**：删除改动后，模型、答案、方案取舍或主要解释是否改变？若都不变，不作为核心亮点。
4. **落地检查**：改动是否进入公式、代码、验证、结果或论文，而非停留在问题分析中的口号？
5. **反证检查**：是否设计了可能推翻该改动的实验、反例或边界情形？

### 4.4 准入与回退

核心改造只有同时满足以下条件才进入最终模型：

- 可靠基线已经闭环；
- 至少两个关键层面存在相互支撑的题目特异性；
- 改动有明确依据并会改变答案、取舍、解释或风险判断；
- 可以在剩余时间内完成实现、验证、图表和论文同步；
- 独立验证或决定性证明支持该改动。

若非显然结论承担降维、可行域缩减、算法构造、方案排除或最优性判断，必须执行 `AGENTS.md` 第 5.3 节的证明准入。证明失败时不得把猜想写成硬约束。

验证失败时：保留探索记录，立即回退可靠基线；不得为了保留所谓创新而更换评价指标、扩大容忍度或删除不利情景。

## 5. 各阶段 AI 的工作方式

### 5.1 读题阶段

只形成任务卡、口径表和题目指纹。没有高影响歧义拍板前，不推荐复杂模型。

### 5.2 定模阶段

先给可靠基线，再给由题目指纹触发的改造；必须说明保留什么、改变什么、为什么改变，以及失败时如何回退。不要列一串模型名让团队自行拼装。

### 5.3 求解阶段

说明求解方法利用了模型的什么结构。若换成任意启发式算法也能原样运行，算法本身不是独特点。结构性降维、分解、界和剪枝若依赖非显然结论，应先证明。

### 5.4 验证阶段

验证应优先攻击独特点，而不是重复证明基础模型能运行。比较基线与改造时使用同一数据、约束、计算预算和评价情景。

### 5.5 论文阶段

独特性按“本题困难—触发证据—具体改造—结果变化—独立验证—适用边界”叙述，不使用“创新性较强、算法先进、效果显著”等无证据评价。

## 6. 核心图的定义

核心图是承担以下任一职责的正文图：

- 直接交付题目所求答案；
- 决定模型、参数或方案选择；
- 揭示无法由表格直接看出的关键机制；
- 验证模型最危险的风险；
- 解释反常、贴边或决定性结果。

图数不设硬上限，但每张核心图必须贡献一条其他图、表或文字没有承担的新结论。探索性图、完整方案明细和调试图原则上放附件或工作记录。

## 7. 核心图工作流

团队在交给 AI 作图前必须提供：

1. 本图拟证明的一句话；
2. 对应题目、模型或验证环节；
3. 数据字段、单位、样本含义和比较口径；
4. 一张手绘关系草图或明确的数据关系描述。

AI 先比较“文字、表格、单图、分面图、空间图、结构图”等载体，必须保留“无需作图”这一候选，不得收到数据后直接批量出图。

## 8. 五项硬门槛

核心图进入正文前必须全部通过：

1. **结论门槛**：能写出“本图用于判断/证明______”的一句话；
2. **证据增量门槛**：删除该图会失去一条关键答案、解释或验证证据；
3. **载体必要门槛**：图比一两句话或小表格更快、更准确地传达关系；
4. **数据诚实门槛**：数据量、指标定义、单位、尺度和不确定性足以支持该图；
5. **题意对应门槛**：图能追溯到题目、模型、求解、结果或验证中的明确位置。

任一项失败，软评分再高也不得作为核心图进入正文。

## 9. 软评分与处理

| 维度 | 分值 | 核查问题 |
|---|---:|---|
| 结论与解释增量 | 30 | 是否产生新的答案、机制解释或验证证据？ |
| 题意与决策对应 | 20 | 是否改变模型、参数、排序、取舍或风险判断？ |
| 数据和语义诚实性 | 15 | 比较是否公平，尺度、单位和样本是否清楚？ |
| 本题特异性 | 15 | 去掉题名后，图的结构是否仍体现本题独有关系？ |
| 阅读效率 | 10 | 评委能否在十秒内找到主要关系？ |
| 视觉克制与团队语法 | 10 | 是否减少装饰并遵守一致视觉编码？ |

- **80–100 分**：可进入正文；
- **65–79 分**：修改后重评，或降级到附件；
- **低于 65 分**：改为表格、文字或删除。

评分只在五项硬门槛全部通过后进行。

## 10. 核心图候选卡

```text
拟支持的一句话结论：
对应题目或模型环节：
使用的数据、单位和指标：
为什么必须用图：
文字或表格替代方案：
本题特异信息：
横轴、纵轴、分组和视觉编码：
可能造成的误读：
与其他图是否重复：
硬门槛：通过 / 不通过，理由
软评分：__/100
处理：正文 / 修改 / 附件 / 表格 / 文字 / 删除
```

## 11. 图型选择规则

- 少量精确值或少量方案：优先表格；两个方案点通常不画所谓权衡散点图。
- 随时间连续变化：时序曲线；事件时刻使用少量垂线或分段，不用大面积背景色代替解释。
- 展示分布、离群或不确定性：箱线、区间、密度或经验分布，不用均值折线假装分布。
- 比较同一对象前后变化：坡度图、哑铃图或配对点，不默认堆叠柱形。
- 排名或贡献：点图或条形图；若贡献依赖消融顺序或不可相加，必须说明定义，不能暗示因果分解。
- 多目标取舍：候选数量足够且存在真实前沿时使用散点/Pareto 图；只有两三个方案时优先表格。
- 空间方案：地图、等高线、路径或分区图；精确坐标和全量方案放附件。
- 模型或问间关系：结构图只画真实依赖、输入输出和新增部分，不画装饰性大框和无信息箭头。
- 求解过程：收敛图只有在说明停止趋势、运行波动或预算影响时保留；一条平滑下降线不能证明全局最优。

## 12. 团队视觉语法

团队已确认采用克制的“深蓝—赭橙—中性灰”主体系，青绿仅作有明确语义的辅助色：

- 主色深蓝 `#2F5D8A`：主体方案、主要曲线和关键结果；
- 强调赭橙 `#C46A3A`：对照方案、风险点或必须强调的位置；
- 中性灰 `#5F6770`：基线、辅助曲线、阈值和次要文字；
- 辅助青绿 `#3F8A75`：仅在确实表达“可行、通过、稳定”等语义时使用，不要求每张图出现。

单张图通常先用蓝、灰两色；确需强调比较时再加橙色。一般不超过三种有意义的颜色，绿色不作为默认第四色。除色板外，固定以下语法：

- 白底或极浅底色；正文图通常使用 2–4 个主要颜色，不用渐变、阴影和立体效果；
- 同一论文中相同语义保持相同颜色或符号，但不同题型不强求所有图套同一版式；
- 多序列同时使用颜色与线型、点型、形状或位置编码，不能只靠颜色；
- 重要线略粗于辅助线，网格线弱化；不让网格、背景区或边框抢过数据；
- 标题说明“画什么”，正文解释“说明什么”；图内只保留读图必需的定义、阈值和关键数值；
- 轴名包含变量含义和单位；小数位与原始数据精度一致；
- 图例优先放在空白区域，能直接在线尾标注时可减少图例往返；
- 颜色不能成为唯一的“好/坏、通过/失败”编码；打印稿中仍应依靠线型、标记或位置区分；
- 一图优先承担一个主要判断，多子图必须共同回答同一个问题。

以下元素默认不用，只有证明存在不可替代的信息增量且不会误导时才允许：

- 双纵轴；
- 大片背景着色；
- 箭头结论框和大段图内说明；
- 无共同问题的多子图拼接；
- 三维柱形、渐变、阴影、发光和装饰图标；
- 超过必要数量的强调色、阈值线和辅助线。

## 13. 三个现场提示

### 13.1 无独特思路时

```text
不要直接推荐模型。请先按专项规程生成题目指纹卡，只找本题独有的数据结构、特殊约束、冲突目标、边界、反常结果、决定性中间结论和验证缺口。按“可能改变模型结构或答案的程度”排序，等待团队选定切口。
```

### 13.2 提出核心模型时

```text
请先给可靠基线，再填写独特性卡。说明题目指纹触发了什么具体改造，至少落在两个关键层面，并设计一个可能推翻该改造的验证。若依赖非显然结论，先执行 AGENTS.md 第5.3节证明准入。不要把算法名称当作创新。
```

### 13.3 准备核心图时

```text
先不要作图。请根据我的一句话结论、手绘草图和数据说明填写核心图候选卡，同时比较文字、表格和两种图型。先过五项硬门槛，再按100分评分；不通过就建议不画。多序列必须同时使用颜色和线型/点型编码。
```

## 14. 终审清单

- [ ] 团队先完成题目指纹，没有由 AI 静默决定关键口径；
- [ ] 可靠基线完整，独特改造至少落在两个相互支撑的关键层面；
- [ ] 每项核心改造能追溯到题面、数据、证明、首版缺陷或验证风险；
- [ ] 独特改造通过独立验证，失败方案已经回退；
- [ ] 决定性非显然结论已经正确证明并实际推进模型；
- [ ] 每张核心图均通过五项硬门槛且评分不低于 80；
- [ ] 每张核心图贡献不可重复的新结论；
- [ ] 少量精确数值没有被强行画成复杂图；
- [ ] 高 AI 味元素若被使用，已有不可替代性和不误导说明；
- [ ] 多序列同时使用颜色以外的冗余编码；
- [ ] 图、表、正文、摘要和最终结果清单采用同一口径与版本。
