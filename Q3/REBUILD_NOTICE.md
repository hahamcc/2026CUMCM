# 旧实验结果失效声明

`Q3/runs/` 中现存结果由口径冻结前的实验程序产生，不属于当前 B / B+ 规格下的有效结果。
这些文件仅为历史审计保留，不得用于论文取数、B与B+比较或正式策略结论。

新的有效结果必须同时满足：

1. 摘要包含明确的 `mode`、配置摘要和代码版本；
2. 完成证书有效且没有区域、覆盖或状态异常；
3. 对应运行使用 `Q3/SPEC.md` 所述共享安全核心；
4. 文件写入新的带版本结果目录，不覆盖旧结果。
